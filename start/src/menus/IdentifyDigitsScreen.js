import React from "react";
import { Text, StyleSheet, View, Image, TouchableOpacity} from "react-native";


const IdentifyDigitsScreen = () => {
  return <View>
      <Text style={styles.text}>IdentifyDigits bitch</Text>
    </View>
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
    textAlign: 'center'
  }
});

export default IdentifyDigitsScreen;